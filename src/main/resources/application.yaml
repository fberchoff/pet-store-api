spring:
  datasource:
    url: jdbc:h2:mem:pet_store;MODE=MYSQL
  h2:
    console:
      enabled: true

    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    
  sql:
    init:
      mode: always